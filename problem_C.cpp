//@author:ReVo
#include <iostream>
#include <cstdio>
#include <cmath>
#include <cstring>
#include <map>
#include <algorithm>
#include <stack>
#include <list>
#include <vector>

//Shortcuts
#define lli long long int
#define fo(i,n) for(i=0;i<n;i++)
#define fi(i,a,n) for(i=a;i<=n;i++)
#define fd(i,n,a) for(i=n;i>=a;i--)
#define modulo 1000000007
#define gi(a) scanf("%d",&a)
#define f(n) for(i=0;i<n;i++)
#define pn printf("\n")
#define pb push_back

using namespace std;

int main()
{
	int t;
	cin>>t;
	int L,X;
	while(t--)
	{
		cin>>L>>X;
		char *input,*populate_input;
		scanf("%s",input);
		strcpy()
		char *str;
		int populate=L;
		while(populate--)
		{
			strcpy(input,str);
			cout<<str;
		}
	}
}